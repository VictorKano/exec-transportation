package com.example.fleet.domain.model;

/**
 * Lifecycle status of a driver profile.
 * Pure Java enum — no Spring, JPA, or framework annotations.
 */
public enum DriverStatus {
    ACTIVE,
    INACTIVE
}
